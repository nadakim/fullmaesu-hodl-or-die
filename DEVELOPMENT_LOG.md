# Development Log

# 풀매수 기원단: HODL or Die

## 프로젝트 개발 기록

> 주식 시장의 급등락과 도파민을 카드 게임으로 재해석한 레트로 2D 트레이딩 게임 프로젝트

---

# 0. 프로젝트 시작

## 개발 목표

주식 시장의 급등과 폭락, 매수와 매도에서 발생하는 심리적 긴장감을 게임으로 구현하고자 했다.

초기에는 Unity 기반 2D 게임을 목표로 했지만, 군복무라는 개발 환경의 제약으로 먼저 사이버지식정보방에서 **HTML / CSS / JavaScript / Canvas 기반 웹 프로토타입**을 제작하여 핵심 게임플레이를 빠르게 검증하는 방향으로 개발을 시작했다.

최종적으로는 웹 프로토타입에서 검증한 게임 시스템을 Unity + C#으로 발전시켜 실제 출시 가능한 게임으로 만드는 것을 목표로 한다.

## 핵심 게임 구조

```text
시장
 ↓
주가 변동
 ↓
플레이어의 매수 / 매도 판단
 ↓
포지션 변화
 ↓
평가손익 변화
 ↓
시장 이벤트
 ↓
카드 / 유물 / 상점
 ↓
다음 라운드
```

---

# 1. 실시간 주식 차트 프로토타입

## 목표

게임의 핵심 화면인 트레이딩룸에 실제 주식 거래 화면과 유사한 움직임을 구현한다.

단순한 숫자 변화가 아니라 주가가 실시간으로 움직이고, 이를 차트로 시각적으로 확인할 수 있도록 설계했다.

## 구현 방향

* 실시간 주가 변화
* Canvas 기반 차트 렌더링
* 상승 / 하락 표현
* 주가 변화에 따른 시각적 피드백

이후 게임의 핵심 차트는 일반적인 선형 그래프가 아닌 **OHLC 캔들스틱 차트**로 발전시키기로 결정했다.

---

# 2. 캔들스틱 차트 구현

## 문제

초기 프로토타입을 발전시키는 과정에서 차트가 원하는 형태의 캔들스틱 차트가 아니라 선형 차트처럼 표시되는 문제가 반복적으로 발생했다.

게임의 핵심 시각 요소가 주식 거래라는 점에서 단순 선 그래프는 원하는 분위기와 기능을 제대로 전달하지 못했다.

## 요구사항 재정의

차트의 기본 단위를 다음과 같이 명확하게 정의했다.

```text
Candle
 ├── Open
 ├── High
 ├── Low
 └── Close
```

상승 캔들:

```text
초록색
```

하락 캔들:

```text
빨간색
```

그리고 이후 개발에서 특별한 요청이 없는 한 **캔들스틱 차트를 선형 차트로 변경하지 않는 것을 핵심 규칙으로 지정**했다.

## 개선 방향

차트 렌더링을 수정할 때 단순히 현재 화면을 비슷하게 만드는 것이 아니라,

* OHLC 데이터 구조
* 캔들 몸통
* 고가 / 저가 꼬리
* 상승 / 하락 색상
* 시간에 따른 캔들 생성

을 유지하도록 개발 기준을 명확하게 했다.

## 배운 점

게임의 핵심 시각 요소는 단순한 디자인 요소가 아니라 **게임 시스템의 일부**로 정의해야 한다는 점을 확인했다.

---

# 3. 매수 기능 구현

## 목표

플레이어가 시장 가격을 기준으로 주식을 매수하고 포지션을 생성할 수 있도록 한다.

핵심 규칙:

```text
매수
 ↓
현금 차감
 ↓
보유 포지션 증가
 ↓
총자산 재계산
```

## 문제

초기 구현에서 매수 기능은 동작하는 것처럼 보였지만 실제 플레이 과정에서 **매수할 때 기존 현금이 제대로 감소하지 않는 문제**를 발견했다.

이 상태에서는 플레이어가 보유 현금보다 훨씬 많은 주식을 계속 매수할 수 있기 때문에 게임의 경제 시스템이 성립하지 않는다.

## 수정 방향

매수 동작을 단순히 "주식 수량 증가"로 취급하지 않고 하나의 거래 처리 과정으로 정의했다.

```text
현재가 × 매수 수량
        ↓
거래 금액 계산
        ↓
현금 차감
        ↓
포지션 증가
        ↓
평가자산 갱신
        ↓
총자산 갱신
```

## 배운 점

게임의 경제 시스템에서는 화면에 보이는 결과보다 **상태 간 관계를 일관되게 유지하는 것이 중요**하다는 점을 확인했다.

---

# 4. 매수할 때마다 주가가 상승하는 문제

## 문제

플레이어가 매수할 때마다 차트의 가격이 상승하는 문제가 발생했다.

이렇게 되면 플레이어의 행동과 시장 가격 변동이 직접적으로 연결되어 플레이어가 매수하는 것 자체가 가격 상승을 만들어내게 된다.

결과적으로 시장의 독립적인 움직임이라는 게임 설계가 무너질 수 있었다.

## 요구사항 재정의

시장 가격은 플레이어의 매수 / 매도와 독립적으로 움직여야 한다.

```text
플레이어 행동
     │
     ├── 현금 변화
     └── 포지션 변화

시장 시스템
     │
     └── 독립적인 가격 변동
```

플레이어의 거래는 자신의 포트폴리오에 영향을 주지만, 시장 가격을 직접 변경하지 않도록 설계 방향을 명확하게 했다.

## 배운 점

게임에서 여러 시스템을 구현할 때 **서로 영향을 주어야 하는 상태와 독립적으로 유지되어야 하는 상태를 명확하게 구분해야 한다**는 것을 경험했다.

---

# 5. 매도 기능 부재 문제

## 문제

매수 기능이 구현된 이후 실제 게임플레이를 테스트하면서 **매도 기능이 제대로 존재하지 않는 문제**를 발견했다.

매수만 가능하면 주식 거래 게임의 핵심적인 의사결정 구조가 성립하지 않는다.

## 요구사항

매도는 다음의 흐름을 가져야 한다.

```text
보유 포지션
 ↓
현재 시장 가격 확인
 ↓
매도 수량 결정
 ↓
청산 금액 계산
 ↓
현금 지급
 ↓
포지션 감소
 ↓
손익 확정
```

또한 매수 시점의 가격과 매도 시점의 가격 차이를 통해 손익을 계산해야 한다.

## 손익 구조

### 평가손익

아직 매도하지 않은 포지션에서 발생하는 현재 기준의 손익.

### 확정손익

실제로 매도하여 포지션을 청산한 후 확정되는 손익.

이 둘을 구분하도록 게임의 경제 시스템을 정의했다.

## 배운 점

기능을 하나씩 추가하는 것보다 **게임의 핵심 루프 전체를 실제 플레이해보는 테스트가 중요**하다는 것을 확인했다.

---

# 6. 카드 선택 시 결과가 미리 보이는 문제

## 문제

카드를 선택할 때 이미 해당 선택의 결과가 이익인지 손실인지 플레이어에게 드러나는 문제가 발생했다.

결과적으로 플레이어가 위험을 감수하고 판단할 필요 없이 유리한 선택만 하면 라운드에서 쉽게 승리할 수 있었다.

## 문제의 본질

단순한 UI 문제가 아니라 **게임의 의사결정 구조 자체가 무너지는 문제**였다.

원래 의도:

```text
카드 선택
 ↓
불확실한 결과
 ↓
시장 상황과 시너지 확인
 ↓
위험 / 보상 판단
```

문제 발생 후:

```text
카드 선택
 ↓
결과를 미리 확인
 ↓
좋은 결과만 선택
 ↓
안전하게 승리
```

## 개선 방향

플레이어가 결과를 사전에 알 수 없도록 하고, 카드 선택과 시장 변화 이후에 결과가 계산되도록 게임플레이 구조를 조정했다.

## 배운 점

게임에서 **정보 공개 시점 자체가 난이도와 재미를 결정하는 핵심 메커니즘**이 될 수 있다는 것을 확인했다.

---

# 7. 랜덤 시장 이벤트 구현

## 목표

게임이 단순히 일정한 속도로 주가가 오르내리는 구조가 아니라, 예상하지 못한 순간에 시장 상황이 급변하도록 만들었다.

## 구현 방향

랜덤한 시간 간격으로 시장 이벤트가 발생하도록 설계했다.

예시:

> "FED 연준 의장이 기침을 했습니다!"

이벤트 발생 시:

```text
평상시 시장
     ↓
랜덤 이벤트 발생
     ↓
시장 상황 급변
     ↓
주가 급등 / 급락
     ↓
플레이어의 판단
```

## 의도

플레이어가 항상 안정적인 시장만 상대하지 않고, 예상하지 못한 급등락에 대응해야 하는 긴장감을 만들기 위한 시스템이다.

---

# 8. 랜덤 이벤트와 시장 시스템의 분리

## 설계 방향

시장 가격은 평상시에도 독립적으로 랜덤 변동하고, 특정 시점에는 이벤트가 별도의 충격을 발생시키도록 구분했다.

```text
기본 시장 변동
+
랜덤 이벤트
=
시장 상황 변화
```

이를 통해 평상시 변동성과 이벤트성 급등락을 구분할 수 있도록 했다.

## 배운 점

랜덤 요소를 하나의 무작위 숫자로 처리하기보다 **기본 시스템과 예외적인 사건을 구분하면 게임플레이를 더 확장하기 쉬워진다**는 점을 확인했다.

---

# 9. 차트 이미지가 검게 변하는 문제

## 문제

게임의 UI와 기능을 수정하는 과정에서 차트의 시각적 표현이 기존과 달라지고, 차트가 검은색에 가까운 형태로 표시되는 문제가 발생했다.

기능 수정 과정에서 정상적으로 작동하던 시각적 요소까지 영향을 받을 수 있다는 것을 확인했다.

## 대응

게임 기능 수정과 시각적 렌더링 수정의 범위를 구분하고, 정상적으로 작동하던 기존 차트 스타일을 유지하는 방향으로 요구사항을 다시 명확하게 정의했다.

## 개발 원칙 추가

이후 기능 수정 시:

> **사용자가 요청한 기능 외에는 기존의 정상 작동하는 UI와 시스템을 임의로 재설계하지 않는다.**

라는 원칙을 개발 기준에 추가했다.

---

# 10. 캔들 차트가 다시 선형 차트로 변경되는 문제

## 문제

차트 관련 기능을 수정하는 과정에서 캔들스틱 차트가 다시 선형 차트로 변경되는 문제가 반복적으로 발생했다.

## 원인 분석 관점

차트는 단순한 장식 요소가 아니라 게임의 핵심 기능이므로, 다른 기능을 수정하면서 차트 렌더링 구조까지 변경되는 것을 방지해야 했다.

## 해결 방향

프로젝트의 명시적인 규칙으로 다음을 고정했다.

```text
Chart Type = OHLC Candlestick
```

그리고 이후 작업에서:

* 캔들 데이터 구조 유지
* 상승 / 하락 색상 유지
* 메인 차트 유지
* 선형 차트로 자동 변경 금지

를 기준으로 삼았다.

## 배운 점

AI를 활용한 개발에서는 자연어 요구사항을 반복해서 전달하는 것만으로는 부족할 수 있으며, **프로젝트의 불변 규칙을 별도의 문서로 명시하는 것이 중요**하다는 점을 경험했다.

---

# 11. 레트로 픽셀 스타일 개선

## 목표

초기 게임은 기능 중심의 프로토타입이었기 때문에 시각적으로 일반적인 웹 UI에 가까운 부분이 있었다.

게임의 컨셉을 강화하기 위해 다음 방향으로 스타일을 발전시켰다.

* 픽셀 폰트
* CRT 느낌
* 레트로 UI
* 강한 초록 / 빨강 증권 색상
* 어두운 배경
* 픽셀 그래픽
* 아케이드 스타일 연출

목표는 단순한 주식 대시보드가 아니라 **레트로 증권 거래소를 게임화한 느낌**을 만드는 것이다.

---

# 12. 배경 차트 애니메이션

## 목표

메인 트레이딩룸이 정적인 화면처럼 보이지 않도록 화면 양쪽에서 주식 차트와 시장 정보가 움직이는 배경 연출을 추가했다.

## 연출 방향

배경 요소가:

* 랜덤 위치에서 등장
* 위 / 아래 / 대각선 방향으로 이동
* 일정 시간 유지
* 자연스럽게 사라짐

하도록 구성했다.

일부 요소는 비교적 오래 유지되고, 일부 요소는 빠르게 등장했다가 사라지는 형태로 구성하여 시장 정보가 계속 흐르는 느낌을 만들고자 했다.

---

# 13. 배경 차트가 너무 적게 나타나는 문제

## 문제

배경 차트 애니메이션을 구현했지만 실제 플레이에서는 요소의 수가 너무 적어 화면이 비어 보이는 문제가 발생했다.

목표했던:

> "수많은 시장 데이터가 끊임없이 움직이는 증권 거래 공간"

보다:

> "가끔 차트가 하나씩 나타나는 화면"

에 가까웠다.

## 개선 방향

배경에 동시에 존재하는 요소의 밀도를 높이고, 새로운 요소가 등장하는 빈도도 높이는 방향으로 수정했다.

또한:

* 작은 차트
* 중간 차트
* 가격 정보
* 거래량
* 등락률
* 종목 코드
* 시장 정보

등 다양한 요소가 랜덤하게 조합되도록 방향을 설정했다.

---

# 14. 왼쪽 상단 고정 차트 제거

## 문제

배경 애니메이션을 추가한 이후 기존 화면 왼쪽 상단에 고정된 장식용 차트가 배경 연출과 겹쳐 화면이 복잡해지는 문제가 있었다.

## 해결 방향

왼쪽 상단의 **고정 장식용 차트만 제거**하고, 게임의 핵심 메인 트레이딩 차트는 유지하도록 요구사항을 구분했다.

이를 통해:

```text
고정 장식 차트
→ 제거

메인 트레이딩 차트
→ 유지

배경 애니메이션
→ 강화
```

하는 방향으로 UI를 정리했다.

## 배운 점

UI 수정에서는 "차트 삭제"처럼 모호한 요구사항보다 **어떤 차트를 삭제하고 어떤 차트를 반드시 유지해야 하는지 명확하게 구분하는 것이 중요**하다는 것을 확인했다.

---

# 15. 배경 연출과 메인 게임플레이의 충돌 방지

배경을 화려하게 만드는 것만으로는 충분하지 않기 때문에 다음 요소들은 항상 우선적으로 보호하도록 했다.

* 메인 트레이딩 차트
* 매수 버튼
* 매도 버튼
* 현금
* 총자산
* 평가손익
* 카드 UI
* 시장 이벤트 UI

배경 애니메이션은 메인 UI 뒤쪽에 배치하고, 화면 중앙의 핵심 정보가 가려지지 않도록 설계 방향을 정했다.

---

# 16. 개발 과정에서 발견한 핵심 문제

현재까지의 개발 과정에서 발견한 주요 문제를 정리하면 다음과 같다.

| 문제                | 개발 과정에서 확인한 중요성     |
| ----------------- | ------------------- |
| 매수 시 현금이 감소하지 않음  | 게임 경제 시스템의 상태 정합성   |
| 매수 시 주가가 상승함      | 시장 시스템과 플레이어 행동의 분리 |
| 매도 기능 부재          | 핵심 게임 루프의 완성        |
| 평가손익 / 확정손익 구분 필요 | 포트폴리오 상태 관리         |
| 카드 결과가 미리 보임      | 정보 공개와 게임 난이도       |
| 랜덤 시장 이벤트 필요      | 게임의 불확실성과 긴장감       |
| 차트가 검게 변함         | 기능 수정과 UI 회귀 문제     |
| 캔들이 선형 차트로 변경됨    | 핵심 시각 시스템 보호        |
| 배경 차트가 너무 적음      | 시각적 밀도와 몰입감         |
| 고정 장식 차트가 남아 있음   | UI 요소 간 역할 구분       |

---

# 17. 개발 과정에서 확립한 핵심 개발 원칙

반복적인 문제를 해결하면서 다음 원칙을 프로젝트의 기준으로 정리했다.

## 1. 기존 기능 우선 확인

새로운 기능을 추가하기 전에 기존 기능이 정상적으로 작동하는지 확인한다.

## 2. 최소 변경

요청한 기능에 필요한 부분만 수정하고 정상적으로 작동하는 기능은 재설계하지 않는다.

## 3. 원인 우선

버그가 발생했을 때 새로운 시스템으로 덮어쓰기보다 기존 로직에서 문제가 발생한 부분을 먼저 찾는다.

## 4. 게임 상태의 일관성 유지

```text
현금
+
포지션 평가액
=
총자산
```

이라는 기본 관계를 유지한다.

## 5. 시장과 플레이어 행동 분리

플레이어의 매수 / 매도가 시장 가격을 직접 변경하지 않도록 한다.

## 6. 핵심 UI 보호

메인 트레이딩 차트와 게임플레이 UI를 배경 연출보다 우선한다.

## 7. 핵심 시스템은 불변 규칙으로 관리

특히 OHLC 캔들스틱 차트처럼 프로젝트의 정체성과 직결되는 요소는 명시적인 개발 규칙으로 관리한다.

---

# 18. AI를 활용한 개발 과정

본 프로젝트는 AI를 개발 도구로 적극 활용한 프로젝트이다.

## 활용 방식

```text
기획
 ↓
게임 시스템 정의
 ↓
AI에게 구현 요구사항 전달
 ↓
프로토타입 구현
 ↓
직접 플레이 테스트
 ↓
문제 발견
 ↓
문제 상황 구체화
 ↓
AI에게 수정 요구
 ↓
다시 플레이 테스트
 ↓
반복
```

AI에게 단순히 "게임을 만들어줘"라고 요청하는 방식에서 점차 발전하여, 실제 개발 과정에서는 **문제 상황과 원하는 동작을 구체적으로 정의하는 방식**으로 요구사항을 개선했다.

## 실제 개발에서 얻은 경험

AI가 코드를 생성하더라도 실제 플레이 과정에서 다음과 같은 문제가 발생할 수 있었다.

* 기존 기능이 변경됨
* 차트 스타일이 변경됨
* 캔들 차트가 선형 차트로 변경됨
* 게임 경제 로직이 의도와 다르게 동작함
* UI 요소가 의도하지 않은 위치에 남음

따라서 AI에게 코드를 생성시키는 것보다 중요한 것은:

> **요구사항을 명확하게 정의하고, 결과물을 직접 테스트하며, 문제가 발생하면 원인을 찾아 다시 수정하는 개발 과정**

이라는 것을 경험했다.

---

# 19. 현재 프로젝트의 기술적 핵심

현재까지의 개발을 통해 다음과 같은 시스템을 구축하고 있다.

### 게임 상태

```text
Cash
Position
Current Price
Portfolio Value
Unrealized P/L
Realized P/L
```

### 시장

```text
Base Market Movement
+
Random Market Event
=
Dynamic Market
```

### 거래

```text
BUY
→ Cash 감소
→ Position 증가

SELL
→ Position 감소
→ Cash 증가
→ Realized P/L 확정
```

### 차트

```text
OHLC
→ Candlestick
→ Canvas Rendering
```

### 게임플레이

```text
시장 변동
→ 플레이어 판단
→ 카드 선택
→ 거래
→ 이벤트
→ 목표 달성 / 파산
```

---

# 20. 향후 개발 계획

## Web Prototype

* [x] 기본 게임 화면
* [x] 실시간 주가 변동
* [x] OHLC 캔들 차트
* [x] 매수
* [x] 매도
* [x] 현금 / 포지션
* [x] 평가손익 / 확정손익 구조
* [x] 랜덤 시장 변동
* [x] 랜덤 시장 이벤트
* [x] 레트로 UI 방향 설정
* [x] 배경 차트 애니메이션
* [ ] 카드 시스템 고도화
* [ ] 유물 시스템
* [ ] 상점 시스템
* [ ] 게임 밸런싱
* [ ] 저장 시스템
* [ ] 최종 웹 프로토타입

## Unity Version

* [ ] Unity 2D 프로젝트 구축
* [ ] C# 게임 상태 관리
* [ ] 캔들 차트 시스템 이전
* [ ] 거래 시스템 이전
* [ ] 시장 시스템 이전
* [ ] 카드 / 유물 시스템 이전
* [ ] UI / 애니메이션
* [ ] 사운드 / VFX
* [ ] 저장 시스템
* [ ] 최적화
* [ ] 플레이테스트

## Release

* [ ] 최종 빌드
* [ ] 플랫폼 결정
* [ ] 게임 소개 영상
* [ ] 스크린샷
* [ ] 배포 페이지
* [ ] 출시

---

# 21. 프로젝트를 통해 얻은 개발 경험

이 프로젝트를 통해 단순히 기능을 구현하는 것뿐 아니라 **프로토타입을 직접 플레이하고 문제를 발견한 뒤 시스템의 요구사항을 다시 정의하는 반복적인 개발 과정**을 경험하고 있다.

특히 다음과 같은 영역을 실제 프로젝트에서 경험했다.

* Canvas 기반 실시간 그래픽
* OHLC 데이터와 캔들스틱 렌더링
* 게임 상태 관리
* 거래 및 게임 경제 시스템
* 평가손익 / 확정손익 관리
* 랜덤 시뮬레이션
* 이벤트 기반 게임플레이
* UI / UX 개선
* 애니메이션 및 시각적 연출
* 버그 발견 및 회귀 테스트
* AI-assisted development
* 요구사항 명세 및 수정
* 프로토타이핑을 통한 게임 시스템 검증

---

# 22. 개발 방향

현재 웹 프로토타입의 목표는 단순히 완성된 웹 게임을 만드는 것이 아니다.

**빠르게 프로토타입을 제작하고 직접 플레이하면서 게임 시스템을 검증한 뒤, 검증된 시스템을 Unity + C# 기반의 실제 게임으로 발전시키는 것**을 목표로 한다.

따라서 현재 발생한 오류와 수정 과정 또한 프로젝트의 실패 기록이 아니라 **게임 시스템을 검증하고 개발 방법을 개선해 나가는 과정의 일부**로 기록한다.
